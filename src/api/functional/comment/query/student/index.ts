/**
 * @packageDocumentation
 * @module api.functional.comment.query.student
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection, Primitive, Resolved } from '@nestia/fetcher';
import { PlainFetcher } from '@nestia/fetcher/lib/PlainFetcher';

import type { CommentQuery } from '../../../../dto/comment/comment-query.dto';

/**
 * @controller CommentController.getStudentCommentWithQuery
 * @path GET /comment/query/student
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getStudentCommentWithQuery(
  connection: IConnection,
  studentQuery: getStudentCommentWithQuery.Query,
): Promise<getStudentCommentWithQuery.Output> {
  return PlainFetcher.fetch(connection, {
    ...getStudentCommentWithQuery.METADATA,
    path: getStudentCommentWithQuery.path(studentQuery),
  } as const);
}
export namespace getStudentCommentWithQuery {
  export type Query = Resolved<CommentQuery>;
  export type Output = Primitive<any>;

  export const METADATA = {
    method: 'GET',
    path: '/comment/query/student',
    request: null,
    response: {
      type: 'application/json',
      encrypted: false,
    },
    status: null,
  } as const;

  export const path = (
    studentQuery: getStudentCommentWithQuery.Query,
  ): string => {
    const variables: Record<any, any> = studentQuery as any;
    const search: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(variables))
      if (value === undefined) continue;
      else if (Array.isArray(value))
        value.forEach((elem) => search.append(key, String(elem)));
      else search.set(key, String(value));
    const encoded: string = search.toString();
    return `/comment/query/student${encoded.length ? `?${encoded}` : ''}`;
  };
}
